import React, { Component } from 'react';
import DemoComponentHeader from './../DemoComponentHeader';
//import styles from "./home.scss"; 

export default class ReactTools extends Component{
		constructor(args) {
        super(args);
        this.state = {
        };
    }


    render() {
    	return (
    		<p>I am areact tools page ping me</p>
    	);
    }

}